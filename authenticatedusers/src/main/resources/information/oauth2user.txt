Roles get updated by deleting all old roles and adding the new ones

Seeded with two users
admin / password - has role admin and user
barnbarn / ILuvM4th! - has role user

See oauth2userddl.png for database layout
See oauth2userrest.pdf for end points documentations

Create maven project with:
jpa
security
web
devtools
h2 (remember to remove the scope - runtime)
actuator

Create
   manually added POM dependencies
   application.properities!
   config
   model
   repository
   service
   controller

   give them seed data!

To run the application - all in PostMan! Port is 8443


Sign on admin / password

Get: /users/getusername
Get: /users/user/{userid}
Get: /users/users

Post: /users/user
{
    "username": "hops",
    "password": "password",
    "userRoles": [
        {
            "role": {
                "roleid": 1,
                "name": "admin"
            }
        },
        {
            "role": {
                "roleid": 2,
                "name": "user"
            }
        }
    ],
    "quotes": [
        {
            "quote": "Hello!"
        }
    ]
}

must now sign in as hops / password

Put: /users/user{id}
{
    "userid": 10,
    "username": "hops",
    "password": "myPassword",
    "userRoles": [
        {
            "role": {
                "roleid": 2,
                "name": "user"
            }
        }
    ],
    "quotes": [
        {
            "quote": "Bye Now!"
        }
    ]
}

must now sign in as admin / password

Post: /quotes/quote
{
    "quote": "p'tak",
    "user": {
              "userid":13
            }
}

Get: /users/users

Delete: /users/user/10

Get: /users/users

Get: /roles/roles

Post: /roles/role
{
    "name" : "client"
}

Delete: /roles/role/2

Note, the testing is not part of this guided part. It is used in just a video!
